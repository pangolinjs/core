<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {% if page.title %}
    <title>{{ page.title }}</title>
    {% endif %}
    {% if page.description %}
    <meta name="description" content="{{ page.description }}">
    {% endif %}

    {{ fesg.head }}

    {% block head %}{% endblock %}
  </head>
  <body class="fesg-body">
    {% if process.env.FESG_ENV == 'dev' or process.env.FESG_ENV == 'build:dev' %}
    {{ fesg.sidebar }}
    {% endif %}

    <div class="fesg-content js-fesg-content">

      {% if page.title or page.description %}
      <header class="fesg-header">

        {% if page.title %}
        <h1 class="fesg-header__title">{{ page.title }}</h1>
        {% endif %}

        {% if page.description %}
        <p class="fesg-header__description">{{ page.description }}</p>
        {% endif %}

      </header>
      {% endif %}

      <main class="fesg-main">
        {% block body %}{% endblock %}
      </main>

      {{ fesg.footer }}
    </div>
  </body>
</html>
